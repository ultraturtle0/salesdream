const { google } = require('googleapis');
const config = require('../../config/config');

var post = (req, res, next) => {
    { bankInfo, cardInfo, otherInfo } = req.body;
    var ledger = [];
    
    //bankInfo
};

    


module.exports = {
    post,
};
